org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Kotlin's in-process incremental compiler cache (FilePageCache) has been
# colliding across concurrently-compiled plugin modules on this machine
# ("Storage for [...] is already registered"). Disabling incremental
# compilation and forcing sequential workers trades a bit of build speed
# for a build that actually completes.
kotlin.incremental=false
org.gradle.workers.max=1
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
