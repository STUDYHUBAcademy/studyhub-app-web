import 'package:flutter/material.dart';

/// StudyHub brand palette — deep navy from the original Maliati prototype
/// blended with the amber accent from the Study Hub tutor-form identity.
class AppColors {
  AppColors._();

  static const background = Color(0xFF0F172A);
  static const surface = Color(0xFF1E293B);
  static const surfaceVariant = Color(0xFF334155);
  static const border = Color(0xFF334155);

  static const accent = Color(0xFFC99A3E);
  static const accentSoft = Color(0xFFEEDDB4);

  static const textPrimary = Color(0xFFF1F5F9);
  static const textMuted = Color(0xFF94A3B8);

  static const success = Color(0xFF4ADE80);
  static const warning = Color(0xFFFB923C);
  static const error = Color(0xFFF87171);
  static const info = Color(0xFF38BDF8);
}
